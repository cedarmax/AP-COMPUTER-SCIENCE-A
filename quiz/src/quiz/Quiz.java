package quiz;

import javax.swing.JOptionPane;

public class Quiz {

	public static void main(String[] args) {
		string number;
		JOptionPane.showInputDialog("Enter a grade between 0-100");
		int x = Integer.parseInt(number);
		if (x > 100); {
			JOptionPane.showMessageDialog(null, "Your grade is " + number);
		}
		

	}

}
